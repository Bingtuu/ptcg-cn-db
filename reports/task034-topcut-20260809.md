# task 034 验收报告：mik topcut_slots 反推物化

日期：2026-08-09 · 任务：tasks/done/034 · 库口径：user_version=11（无迁移）· PRD v1.19

## 1. 背景

task 028/031 遗留技术债：mik 源 26 场赛事 `tournaments.topcut_slots` 全 NULL（`normalize/tournaments.py` 写死 None，mik 赛事端点本身不给名额数）。统计层 `winrate_b.sql` / `wws.sql` 硬过滤 `topcut_slots IS NOT NULL` → **CN 样本 B 层胜率（top-cut 转化率 CR）与 WWS 完全空集**（WUR 不受影响）。反推信号早已在本地：`deck-static-by-tour` raw 每 variant 带 `topcutTimes` 五档累计数组 `[冠军, top2, top4, top8, top16]`——PRD FR-9.4 指定的 B 层对账锚。

## 2. 口径与校验链

**物化口径**：topcut_slots = `topcutTimes` 最外档（top16 档）列向合计（逐 variant 外档值求和）。

**物化前置校验链（全部满足才写库，否则跳过/转 question，不猜）**：

- `participant_count > 0` 且非 `is_team`（0 人场无意义；双卡组团队赛 topcutTimes 是人均口径，与个人赛不可比）；
- 五档单调不减（内档 ≤ 外档，累计数组结构校验）；
- 外档合计 ≤ participant_count；
- **外档 ∈ {4, 8, 16, 32} 常见档位**（3348 的 [1,2,5,10,19] 被这条拦截转 question）；
- deck-static 空 list → 跳过（NULL 如实保留）；is_qual 场照物化（数据是事实，统计层默认排除 qual 是查询侧行为）。

**写库纪律**：只写 NULL→值，永不覆盖已有值（幂等、单调）；产出物化/跳过/question 三清单。

## 3. 实现形态（历史与增量一套代码）

- `ptcgdb/normalize/topcut.py`（新）：`derive_topcut_slots()` 读本地 raw 逐场校验+反推，update 物化；`--fetch` 路径对 deck-static 为空的场次 force 重抓单端点（空 list 文件 hash 有效会被断点续传跳过，必须 force；限速照 FR-9.5 2s/请求）。
- CLI `ptcgdb backfill-topcut [--fetch]`：历史一次性回填。
- `ingest-tourneys` 尾部钩子调用同一函数：新赛事入库后自动物化，历史与增量一套代码。
- 评审修复（commit 12a58b6）：refetch 熔断处理对齐邻居命令 + 查询收窄（topcut_slots NULL / 非 team / 人数>0）+ ingest 钩子产出并入 topcut.warnings。
- PRD v1.19 先行：§7.5 topcut_slots 口径补 mik 源物化路径（deck-static topcutTimes 外档合计 + 结构校验不猜）。

## 4. 实测结果（真实库，2026-08-09）

备份：`.scratch/ptcg-cn-before-task034-20260809.db`。

### 4.1 backfill-topcut --fetch 首跑

`materialized=9 skipped=16 question=1 warnings=7`。

- **物化 9 场，topcut_slots 全 = 16**：3210 / 3211 / 3215 / 3216 / 3307 / 3342 / 3343 / 3462 / 3470（SQL 对账逐场核实）。
- **question 唯一 1 条**：`mik_moe:3348 最外档 19 不在合法名额集合 [4, 8, 16, 32]`（10,461 人大赛 topcutTimes=[1,2,5,10,19] 结构异常，保持 NULL 不猜，与拍板一致）。
- **warnings 7 条**：3463 / 3464 / 3465 / 3466 / 3467 / 3468 / 3471 双卡组赛（topcutTimes 人均口径）跳过。
- **8 场 qual 高级赛（3301/3302/3304/3305/3309/3310/3312/3320）全部「+ 重抓」**：raw 已更新（fetched_at 2026-08-09T14:50Z）但 mik 源 `data.list` 仍为空 → 保持 NULL 正确跳过（重抓动作执行、结果如实）。
- 计数闭环：26 场 = 物化 9 + question 1 + skipped 16（8 qual 空 + 7 双卡组 + 3469 无 topcutTimes 静默跳过）。
- NULL 残留对账：3348 / 3463~3469 / 3471 共 9 场全 NULL（与口径逐场对应）。

### 4.2 幂等复跑（无 --fetch）

`materialized=0 skipped=25 question=1 warnings=7`，零网络请求——只写 NULL 不覆盖语义验证通过。

### 4.3 B 层统计首次非空（CN 样本）

- `stats winrate --layer b`：**283 数据行**（meta n_tournaments=5，q0=0.1249，mirror=exclude）；高 n 行：老大的指令 0.4173 n=261、奇树 0.4078 n=249。
- `stats wws --layer b`：**289 数据行**；头部：老大的指令 0.2493 n=324、奇树 0.1997 n=305、吉雉鸡ex 0.1912 n=268。
- 说明：n_tournaments=5 而非 9 是统计层默认窗口（date_from=2026-05-11 + include_qual/team=False）的查询侧行为，非回填问题。

## 5. 已知残留（如实保留，均为口径内跳过非缺陷）

- `mik_moe:3348`：question 保持 NULL（最外档 19 结构异常，不猜；待官方赛报人工核实后可走人工登记路径）。
- 8 场 qual 高级赛（3301~3320）：`--fetch` 重抓后 mik 源 data.list 仍空 → NULL；源端确实无 deck-static 数据。
- `mik_moe:3469`：0 人场无 topcutTimes，静默跳过。
- 7 场双卡组赛（3463~3468/3471）：人均口径与个人赛不可比，NULL 保留。

## 6. 测试

589 测试全绿（573→589），ruff 全净。TDD 覆盖：标准形态物化 / 异常结构拦截（3348 型）/ 团队赛跳过 / 空 static 跳过 / 0 人场跳过 / qual 照物化 / 只写 NULL 不覆盖 / CLI 与 ingest 钩子。
