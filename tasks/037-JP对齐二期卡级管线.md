# 037 · JP对齐二期卡级管线

| 项 | 内容 |
|---|---|
| 状态 | TODO（前置：task 036） |
| 关联 | PRD FR-9.1a / FR-9.5（v1.20 红线定向放宽 + 成本守卫）、里程碑 M10、task 035 报告 |
| 预估 | 3~4 天 |

## 目标

JP 对齐二期卡级管线：聚合站壳（店名/日期/名次/archetype）+ 官方卡组码 → pokemon-card.com deck confirm 页解析卡表 → JA 名映射简中卡 → 赛事四表入库（JP 专用通道）→ `basis=jp` 卡级统计（WUR/WR/WWS）解锁。

## 已拍板口径（2026-08-14，PRD v1.20）

- **红线定向放宽**：仅 `/deck/confirm.html/deckID/{码}` 端点，仅聚合站收录的 JP 对齐窗口内官方赛事上位卡组码；限速 5s/请求 + 熔断 + 逐码断点续传 + 请求台账；raw append-only。其余官方站页面维持抽样红线（≤35 请求、≥2s/请求）不变。
- **成本守卫**：采集前先出请求量估算；估算 > 500（默认闸门，开工时可拍板调整）→ 降级只收**最高等级场次**（PJCS / チャンピオンズリーグ），窗口 = JA 旋转日历 GHI 段 2025-01-24 ~ 2026-01-22（含 GHIJ 过渡期），即对齐当前简中 G/H/I 环境的数据。
- 最终判据 = 卡级映射 full（FR-9.1a），日期窗口仅成本先验；统计仅消费 full。

## 步骤

- [ ] 聚合站采集器：pokecabook 主（卡组码密度最高，444 个/篇）+ pokecardlab 互核（日期型 URL 窗口定位最直接）；提取壳字段 + 卡组码，断点续传，raw 落盘
- [ ] 卡组码清洗：去重 + 窗口过滤 + 赛事分级（PJCS/CL/City League…）→ **请求量估算报告 → 成本守卫闸门判定**（必要时降级最高等级，判定留痕）
- [ ] deck confirm 解析器：纯正则解析隐藏 input 8 分组（`deck_pke/gds/tool/tech/sup/sta/ene/ajs`）+ `PCGDECK.searchItemName` 卡名表（含 `(SET 编号/总数)`）+ 卡图路径 JP 系列码辅助信号（task 035 已实测复现，无需 Playwright）
- [ ] 官方站采集执行：5s/请求、熔断、逐码断点续传、请求台账
- [ ] ingest：JA 官方名 → 简中卡映射（name_ja 桥 + task 036 词表 + (JP set,number) 辅助信号，多候选不猜）；mapping_status 分档、deck_card_misses 兜底、60 张质量门、tournaments.env 推导（JA 日历段）+ 卡组最大赛制标记交叉校验
- [ ] tier 词表 JP 赛事档位落地（PJCS=4 / CL=2 复用既有档，City League 等新档如收）
- [ ] `basis=jp` 统计接入（枚举已预留；canonical SQL `:basis` 冒烟）
- [ ] `monitor tourneys` 增 jp 源编排（增量随刷新管线，FR-9.8）
- [ ] 对账：pokecabook × pokecardlab 两站壳互核（场次/名次/archetype）+ 抽样人工核对卡表

## 验收标准

- [ ] 成本守卫执行留痕：请求量估算数字 + 闸门判定结果入报告
- [ ] 窗口期入库卡组 full 率 ≥90%（task 035 预估复核）
- [ ] 两站壳互核一致率报告；映射未解项全量归类入 misses
- [ ] 官方站请求台账：全部 ≥5s 间隔，总量与估算对平
- [ ] 测试全绿 + ruff 全净；报告归档 `reports/`；STATUS/CHANGELOG 同步

## 完成总结（DONE 时填写）
