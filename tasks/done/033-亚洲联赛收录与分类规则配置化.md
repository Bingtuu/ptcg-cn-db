# task 033：亚洲联赛收录与分类规则配置化

- 状态：DONE
- 创建：2026-08-09
- 完成：2026-08-09
- 里程碑：M9-3 后续补强（task 028 遗留拍板）
- 前置：task 028（Limitless 双通道）、task 031（窗口守卫/刷新编排）、task 032（misses/remap）

## 背景与拍板（2026-08-09，用户对 task 028 遗留的决策）

task 028 主站通道 reject 清单剩 9 场未收（Worlds 2025 已在 task 032 补录）：

- **Master Ball League 3 场**：Singapore（id=511，2025-06-28，524 人）/ Philippines（506，2025-04-26，515 人）/ Malaysia（505，2025-04-19，1017 人）
- **Premier Ball League 3 场**：Indonesia（557，2025-11-22，827 人）/ Malaysia（564，2025-09-20，1250 人）/ Philippines（555，2025-09-06，799 人）
- **Korean League 3 场**：S4（504，2025-04-12，308 人）/ S2（561，2026-01-10，387 人）/ S3（562，2026-03-07，382 人）

拒收本是范围收口（FR-9.1a「JP 国内赛事拒收」+ 兜底分支），非技术障碍——这 9 场用 EN 卡名，映射链零改动可走通。真正的技术门槛是 JP 卡赛事（Japan Championships/Champions League/JCS，日文火卡名 EN 桥走不通），不在本轮范围。

用户三问拍板：

1. **收录口径**：全部 9 场 EN 卡亚洲联赛都收。
2. **tier 设计**：三档独立词表词条，对标官方定位——`master_ball_league` coef=1.5、`korean_league` coef=1.5（顶替原 Regional 级）、`premier_ball_league` coef=1.0（顶替原 League Cup 级）。
3. **框架形态**：主站分类规则配置化（采集+入库共用单一事实源），今后新联赛/截断调整 = 改配置零代码；不加 country 列。
4. **补充（2026-08-09）**：Korean League 卡组若无法对应英文名，该部分先不处理——映射失败走既有 misses 记录，不为此改映射链。

## 设计

### A. 分类规则配置化（`config/site_tournament_rules.yml`，新）

单一事实源，合并现在分离的 SITE_TIER_PATTERNS / SITE_CUT_LIMITS / JP_DOMESTIC_PATTERN 三个代码常量：

```yaml
min_players: 32
tiers:                  # 收侧：tier 正则与名次截断合一，消除「新 tier 忘配 cut」的坑
  - tier: worlds
    patterns: ["World Championships"]
    cut_limit: 32
  - tier: international
    patterns: [...]     # NAIC/EUIC/LAIC/OCIC 原样迁入
    cut_limit: 32
  - tier: master_ball_league
    patterns: ["Master Ball League"]
    cut_limit: 32
  - tier: korean_league
    patterns: ["Korean League"]
    cut_limit: 32
  - tier: premier_ball_league
    patterns: ["Premier Ball League"]
    cut_limit: 8        # 对标 league_cup 档
  # special / regional / league_cup 原样迁入
reject:                 # 拒侧：明细化理由，只留真 JP 卡赛事
  - pattern: "Japan Championships|Champions League|JCS"
    reason: "JP 卡国内赛，EN 桥走不通，JP 对齐二期再议"
```

- 判定顺序不变：人数门 → 收侧按序 → 拒侧按序 → 兜底拒（「未命中官方系列赛名称正则」）。
- 加载时校验：每档必须有 patterns+cut_limit；正则编译检查；tier 名必须在 tournament_tiers.yml 词表内。校验失败 fail-fast。
- Korean/Premier Ball League 从拒侧挪收侧；拒侧保留 Japan Championships/Champions League/JCS。

### B. 词表三词条（`config/vocabularies/tournament_tiers.yml`）

master_ball_league=1.5、korean_league=1.5、premier_ball_league=1.0，各带 aliases——照 worlds 模板（系数为拍板值，注释留对标论证）。

### C. 代码改动（最小面）

- `ptcgdb/scrapers/limitless_site.py`：`classify_site_tournament()` 改读配置（路径可注入便于测试）；删三个常量。
- `ptcgdb/normalize/ingest_limitless_site.py`：cut limit 消费端同读配置；runner/ingest 两调用点签名不变。
- API 通道（`scrapers/limitless.py` TIER_PATTERNS）不动——只收平台线上赛，与亚洲联赛无关。
- `monitor/tourneys.py` 不动——配置生效后增量刷新自动带新口径。

### D. 历史回填

9 场当时在采集侧被 reject（未抓 standings/卡组页）：重跑 `scrape limitless-site`（断点续传，只补抓新接受场次）→ `ingest-limitless-site`（幂等，既有 40 场不变）。日期 2025-04-12~2026-03-07 全在对齐窗口 (2025-04-11, 2026-04-09) 内，守卫不拦。raw append-only 旧 run 不动。预期 tournaments 40→49；真实库操作前备份 `.scratch/`。

### E. PRD v1.18

- FR-9.1a 口径句改写：EN 卡官方系列赛含亚洲联赛（MBL/KL/PBL）；JP 卡国内赛（Japan Championships/Champions League/JCS）仍拒收，JP 对齐二期。
- 新增规则配置化条款（site_tournament_rules.yml 单一事实源）。

## 步骤

- [x] 1. PRD v1.18 先行（FR-9.1a 改写 + 配置化条款）
- [x] 2. `config/site_tournament_rules.yml` + 加载/校验模块（TDD：缺 cut_limit/非法正则/未知 tier 报错）
- [x] 3. `classify_site_tournament()` + ingest cut limit 改读配置；词表三词条（TDD：9 场名→正确 tier、JP 卡赛仍拒、既有分类零回归）
- [x] 4. 重跑 scrape + ingest 回填 9 场（先备份真实库）；对账 scraped.json 与库计数
- [x] 5. 全量测试 + ruff；full/partial/misses 实测分布记录（Korean League 映射率观察，不处理）
- [x] 6. 验收报告 + CHANGELOG/STATUS/AGENTS 同步 + 归档

## 验收标准

- [x] 9 场亚洲联赛入库，tier/tier_coef 正确（MBL/KL=1.5、PBL=1.0），topcut_slots 按配置截断物化——实测：MBL SG/PH/MY = 31/17/14、PBL ID/MY/PH = 8/8/5、KL S4/S2/S3 = 21/29/28，全 ≤ 截断档（32/32/8），env 全 GHI
- [x] 主站通道分类规则 100% 配置驱动（limitless_site 侧无常量残留；API 通道 TIER_PATTERNS 明确不动），既有 40 场分类零回归——实测：regional 26@1.5 / special 10@1.5 / international 3@4.0 / worlds 1@6.0 不变，grep 四常量名 ptcgdb/ 零残留
- [x] 未映射条目全部落 deck_card_misses（Korean League 若有未解缺口只记录不处理）——实测：未解 misses 1,456→1,592 行（+136）/ 40 distinct 名全 no_cn_printing；新三 tier 150 行/23 名（KL 123/20、PBL 27/10、MBL 0）
- [x] 全量测试绿 + ruff 净；真实库对账 40→49 场且既有数据零漂移——573 测试全绿、ruff 全净；scrape accepted=49/rejected=0、ingest blocked=0/skipped_out_of_window=0
- [x] PRD v1.18 + CHANGELOG + STATUS + AGENTS 同步，任务归档——报告 `reports/task033-asia-leagues-20260809.md`

## 完成总结（DONE 时填写）

**做了什么**：PRD v1.18 先行（FR-9.1a 亚洲联赛口径 + 配置化条款）→ tier 词表三档（MBL/KL=1.5、PBL=1.0）→ `config/site_tournament_rules.yml` 单一事实源 + `scrapers/site_rules.py` fail-fast 加载校验 → classify/runner/ingest 三消费点改读配置（删四常量）→ 备份后断点续传重跑 scrape + ingest 回填 9 场 → 文档同步与归档。

**验收结果**：六步全过、五条验收标准逐条核销（实测数字见上勾选项）。真实库终态 limitless_site 40→49 场、全库 80 赛、site 通道 full 549/partial 546；573 测试全绿、ruff 全净。验收报告 `reports/task033-asia-leagues-20260809.md`。

**与预估偏差**：基本无偏差——9 场如预估全走通既有映射链零改动；MBL 三场缺口实测 0 行（2025 上半年卡全有简中对应），KL/PBL 缺口全为 Mega 时代 no_cn_printing，与设计预期一致。

**遗留**：①mik 26 场 topcut_slots 仍 NULL（task 031 旧债，不在本任务范围）；②Korean League 等未解缺口待半年后简中进 Mega 环境由 L0 remap 钩子 / `remap-decks` 整体刷新（partial→full 单调升级机制 task 032 已实战验证）；③JP 卡国内赛仍拒收，JP 对齐二期再议。
