# 038 · 效果标签词表定稿

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §6.4（v1.22）、里程碑 Phase 3；spec `docs/superpowers/specs/2026-08-16-phase3-effect-tags-design.md`（定稿）；plan `docs/superpowers/plans/2026-08-16-task038-effect-tags-vocab.md` |
| 预估 | 1 天 |

## 目标
23 意图标签 + 3 机制 flag 词表落 `config/vocabularies/effect_tags.yml`（fail-fast loader + matcher + 命中率评测），当前环境（standard GHI）零命中全归类后用户拍板词表 v1；PRD §6.4 上限修订同步。

## 步骤
- [x] PRD v1.22 §6.4 修订（上限 23+3 开放追加 + effect_tags 结构 + 扩展流程）
- [x] `ptcgdb/mapping/effect_tags.py` loader + matcher（TDD）
- [x] 词表 yml 首版 + 种子用例测试（spec 抽查 7 卡 + 查漏派生用例）
- [x] scan harness + CLI `tag-effects-scan` + 命中率报告（只读零写入）
- [x] 实跑迭代：GHI 卡池零命中逐条归类（三出口）至零未知项；全库 `--all` 参考扫描
- [x] 用户拍板词表 v1 → CHANGELOG + 完工同步

## 验收标准
- [x] 词表 v1 经用户拍板（命中率报告驱动）
- [x] 当前环境卡池零命中全归类（无标签合理 / 词表缺口已修），零"不知道是什么"项
- [x] 命中率报告落 `reports/`（分标签计数 + 多命中审视 + 零命中归类）
- [x] 扩展性测试锚定：新增标签 = 只改词表 yml，matcher 零改动生效
- [x] 测试全绿 + ruff 全净；PRD §6.4 与词表口径一致

## 完成总结（DONE 时填写）

- 做了什么：词表 v1 定稿 `config/vocabularies/effect_tags.yml`（**28 意图标签 + 3 机制 flag**；初版 23，GHI 实测零命中浮出 5 新类别 coin_manipulate/bench_attack/win_condition/special_summon/counter_shift_self，用户拍板 2026-08-17 转正追加，纯 yml 零代码）；纯函数引擎 `ptcgdb/mapping/effect_tags.py`（loader fail-fast + match_tags/match_flags 不变式）+ CLI `tag-effects-scan`（--day/--fmt/--sets/--all，只读，自动写报告）；评测报告含评测口径、锚例子小样本、零命中逐条归类节（全归档不入库）、范围声明。
- 验收逐条结果：①28 标签每标签 ≥3 种子自测全过（94 词表测试全绿）；②GHI 1,507 文本 171 条零命中逐条归类全过（锚例后无新增结构类别，剩余全部"无需打标"）；③41 条锚例逐一命中断言全过；④报告含评测口径/锚例样本/归类节/范围声明四要素 ✅；⑤`--all` 实测 4,176 文本覆盖 84.9%（≥80% ✅）。
- 终态数字：GHI texts=1507 covered=1336（88.7%）multi=97 zero=171；全库 --all texts=4176 covered=3547（84.9%）multi=244 zero=629。
- 与预估偏差：预估 1 天，实际跨 08-16~08-17 两天（含两轮 review 修正清偿：R1 归类节落报告 + yml 注释收口，R2 lock/energy_disrupt 变体补收 4 条种子）。
- 遗留风险：--all 旧机制零命中 629 条为已知信息项不追；多命中 97 条为系统性共命中维持口径；task 039 接口约定 = 复用 `load_effect_vocab/match_tags/match_flags` + `detail{attacks/ability/text/flags}` 结构，标注器落库待开工。
